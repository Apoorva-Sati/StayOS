spring:
  application:
    name: stayos-backend
  datasource:
    url: jdbc:postgresql://localhost:5432/stayos
    username: stayos
    password: stayos_local_dev
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080
