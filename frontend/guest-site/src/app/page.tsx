export default function Home() {
  return (
    <main className="p-10">
      <h1 className="text-2xl font-semibold">Book direct, skip the OTA fee</h1>
      <p className="mt-2 text-gray-600">
        Room search and the booking flow land in week 2 - this route is a
        placeholder confirming the guest-site app runs.
      </p>
    </main>
  );
}
