services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: stayos
      POSTGRES_USER: stayos
      POSTGRES_PASSWORD: stayos_local_dev
    ports:
      - "5432:5432"
    volumes:
      - stayos_pgdata:/var/lib/postgresql/data

volumes:
  stayos_pgdata:
